services:
  ssh:
    build:
      context: tests/fixtures/debian-sshd
    image: ssh-mcp-debian-sshd:latest
    ports:
      - "127.0.0.1:2222:2222"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
